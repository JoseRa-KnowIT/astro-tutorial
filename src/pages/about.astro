---
import '../styles/global.css';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import BaseLayout from '../layouts/BaseLayout.astro';

const pageTitle = "Acerca de esta madre!"

const identity = {
	firstName: "José Ramón",
	country: "México",
	occupation: "Enginner",
	hobbies: [ "DJing", "Learning", "Drinking" ]
}
const skills = ["Todos"]
const happy = false;
const finished = false;
const goal = 4;

const skillColor = "navy"
const fontWeight = "bold"
const textCase = "uppercase"
---

<BaseLayout pageTitle={pageTitle}>
	<p>
		Esto es parte del mendigo tutorial introductorio de Astro. Estamos en una
		segunda pagina.
	</p>
	<p>
		El sitio se ira actualizando poco a poco aunque a nadie le importe lo quepase aqui!
	</p>
	<p>Here are a few facts about me:</p>
	<ul>
		<li>My name is {identity.firstName}.</li>
		<li>I live in {identity.country} and I work as a {identity.occupation}.</li>
		{identity.hobbies.length >= 2 &&
			<li>Two of my hobbies are: {identity.hobbies[0]} and {identity.hobbies[1]}</li>
		}
	</ul>
	<p>My skills are:</p>
	<ul>
		{skills.map((skill) => <li class="skill">{skill}</li>)}
	</ul>
	{happy && <p>I am happy to be learning Astro!</p>}

	{finished && <p>I finished this tutorial!</p>}

	{goal <= 3 ? <p>My goal is to finish in less than 3 days.</p> : <p>My goal is not 3 days.</p>}
</BaseLayout>

<style define:vars={{skillColor, fontWeight, textCase}}>
	.skill {
		color: var(--skillColor);
		font-weight: var(--fontWeight);
		text-transform: var(--textCase);
	}
</style>